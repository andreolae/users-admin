import * as React from "react";
import { Card, CardContent, CardHeader } from '@mui/material';

const Dashboard = () => (
    <Card>
        <CardHeader title="Bem-vindo, Administrador!"/>
        <CardContent >
            
        </CardContent>
      
    </Card>
);

export default Dashboard;